import { MessageListener } from "./message-module/message-listener";
import { ProfileListener } from "./profile-module/profile-listener";
const messageListener=new MessageListener();
const profileListener=new ProfileListener();

